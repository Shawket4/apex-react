# Repair pass — shared group

| Item | Status | Notes |
|---|---|---|
| R01 | applied | Deleted `role={onRowClick \|\| canExpand ? 'button' : undefined}` from the row `<tr>`; `tabIndex`, `aria-expanded`, `onClick` and `onKeyDown` kept, so rows stay keyboard-operable while regaining native `row`/`cell` table semantics — src/shared/ui/data-table.tsx:233 |
| R02 | applied | Removed `role="button"`, `tabIndex={0}` and `aria-expanded` from the header `<div>` (attributes only). The inner `<button aria-expanded>` is now the single control and single tab stop; the wrapper keeps `onClick={toggle}`, which the button's click still bubbles to, so the toggle fires exactly once from mouse and keyboard. `onKeyDown` handler left in place (its `e.target === e.currentTarget` guard now never matches, but the constraint forbids deleting it). Comment added explaining the arrangement — src/shared/ui/collapsible-section.tsx:67-72 |
| R05 | applied | Collapsed the three dead ternaries to the surviving class: `className="text-muted-foreground"` at :611, and `text-muted-foreground` folded into the `cn(...)` base string at :619 and :684. `tripActive` / `active` are still used elsewhere in the file (tsc clean). Only the time-deck sites are in this group — fuel-events-filters.tsx:221 and google-provider.tsx:773 belong to other groups — src/features/tracking/components/time-deck.tsx |
| R07 | applied | `zDateString` now uses `.refine((v) => /^\d{4}-\d{2}-\d{2}$/.test(v), () => ({ message: i18n.t(...) }))` instead of `.regex(..., { message })`, so the message resolves lazily per validation and follows a runtime language switch like the three sibling validators. Export name, type (`ZodString` → `ZodEffects<ZodString>`, still `string`) and accepted values unchanged; tsc clean — src/shared/lib/zod-utils.ts:29-33 |
| R19 | applied | Added the four missing Arabic plural categories beside the existing pair: `countLabel_zero` ("لا مصاريف"), `countLabel_two` ("مصروفان"), `countLabel_few` ("{{count}} مصاريف"), `countLabel_many` ("{{count}} مصروفًا"), mirroring the `fleetExpenses.cashIn.strip_*` shape. Base `countLabel` covers `one`, `_other` unchanged. English needs no change — src/shared/i18n/locales/ar.json:524-529 |
| R21 | skipped | The item itself states "No edit needed in stat-card.tsx": `valueClassName` already exists (:24/:129/:155/:158/:161) and is wired to every value slot, so the false premise that blocked shards 007/008/010/012/015 is already refuted by the current file. The optional `tone: 'money'` member was not added — it would be an unused enum arm, and the unblocked repairs (R22–R26) all specify `valueClassName="font-mono text-money"`, which the primitive already supports. The per-file fixes are in other groups. |

APPLIED: 5 SKIPPED: 1
